#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n
    while(n--) {
        int t;
        cin >> t;
        int arr[z];
        for (int i = 0; i < 2 * t; i++)
            cin >> arr[i];

        
    }
    return 0;
}