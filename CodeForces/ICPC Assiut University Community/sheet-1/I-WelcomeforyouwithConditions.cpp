#include <bits/stdc++.h>
#include <cmath>
using namespace std;
#define BOOST ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)

int main() {
    BOOST;
    int a, b;
    cin >> a >> b;
    if(a>=b)
        cout << "Yes";
    else
        cout << "No";
    return 0;
}